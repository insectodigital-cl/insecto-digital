---
// index.astro - Insecto Digital Homepage
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Insecto Digital — Agencia Creativa</title>
  <meta name="description" content="Agencia creativa digital que construye soluciones para el mundo digital. Diseño, branding, marketing y comunidad." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;700;800&family=DM+Sans:ital,wght@0,300;0,400;1,300&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --negro: #080808;
      --verde: #BCFF47;
      --magenta: #FF3CAC;
      --cyan: #2AFADF;
      --naranja: #FF6B2B;
      --blanco: #F5F0E8;
      --gris: #1A1A1A;
      --gris2: #2C2C2C;
      --font-display: 'Syne', sans-serif;
      --font-body: 'DM Sans', sans-serif;
    }

    html { scroll-behavior: smooth; }

    body {
      background: var(--negro);
      color: var(--blanco);
      font-family: var(--font-body);
      font-weight: 300;
      overflow-x: hidden;
      cursor: none;
    }

    /* CURSOR PERSONALIZADO */
    .cursor {
      width: 12px; height: 12px;
      background: var(--verde);
      border-radius: 50%;
      position: fixed;
      pointer-events: none;
      z-index: 9999;
      transform: translate(-50%, -50%);
      transition: width 0.2s, height 0.2s, background 0.2s;
      mix-blend-mode: difference;
    }
    .cursor-follower {
      width: 36px; height: 36px;
      border: 1px solid rgba(188,255,71,0.4);
      border-radius: 50%;
      position: fixed;
      pointer-events: none;
      z-index: 9998;
      transform: translate(-50%, -50%);
      transition: all 0.12s ease-out;
    }
    body:hover .cursor { opacity: 1; }

    /* NAV */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1.5rem 4rem;
      mix-blend-mode: difference;
    }
    .logo {
      font-family: var(--font-display);
      font-weight: 800;
      font-size: 1.1rem;
      color: var(--blanco);
      text-decoration: none;
      letter-spacing: -0.02em;
    }
    .logo span { color: var(--verde); }
    nav ul {
      list-style: none;
      display: flex;
      gap: 2.5rem;
    }
    nav ul a {
      color: var(--blanco);
      text-decoration: none;
      font-size: 0.8rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      opacity: 0.7;
      transition: opacity 0.2s;
    }
    nav ul a:hover { opacity: 1; }
    .nav-cta {
      font-family: var(--font-display);
      font-weight: 700;
      font-size: 0.75rem;
      color: var(--negro) !important;
      background: var(--verde);
      padding: 0.5rem 1.25rem;
      border-radius: 100px;
      opacity: 1 !important;
      letter-spacing: 0.05em;
      text-transform: uppercase;
    }

    /* HERO */
    .hero {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      padding: 0 4rem 5rem;
      position: relative;
      overflow: hidden;
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      z-index: 0;
    }
    .blob {
      position: absolute;
      border-radius: 50%;
      filter: blur(80px);
      opacity: 0.18;
      animation: float 8s ease-in-out infinite;
    }
    .blob-1 {
      width: 600px; height: 600px;
      background: var(--verde);
      top: -100px; right: -100px;
      animation-delay: 0s;
    }
    .blob-2 {
      width: 400px; height: 400px;
      background: var(--magenta);
      bottom: 100px; left: 10%;
      animation-delay: -3s;
    }
    .blob-3 {
      width: 300px; height: 300px;
      background: var(--cyan);
      top: 30%; left: 40%;
      animation-delay: -6s;
    }

    @keyframes float {
      0%, 100% { transform: translate(0, 0) scale(1); }
      33% { transform: translate(30px, -20px) scale(1.05); }
      66% { transform: translate(-20px, 15px) scale(0.95); }
    }

    .noise-overlay {
      position: absolute;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='1'/%3E%3C/svg%3E");
      opacity: 0.035;
      pointer-events: none;
    }

    .hero-label {
      font-size: 0.7rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--verde);
      margin-bottom: 1.5rem;
      position: relative;
      z-index: 1;
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    .hero-label::before {
      content: '';
      display: block;
      width: 32px; height: 1px;
      background: var(--verde);
    }

    .hero-title {
      font-family: var(--font-display);
      font-weight: 800;
      font-size: clamp(4rem, 10vw, 9rem);
      line-height: 0.92;
      letter-spacing: -0.04em;
      position: relative;
      z-index: 1;
      max-width: 900px;
    }
    .hero-title .line-accent { color: var(--verde); }
    .hero-title .line-stroke {
      -webkit-text-stroke: 1px var(--blanco);
      color: transparent;
    }

    .hero-bottom {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      margin-top: 4rem;
      position: relative;
      z-index: 1;
    }
    .hero-desc {
      max-width: 340px;
      font-size: 1rem;
      line-height: 1.6;
      opacity: 0.65;
    }
    .hero-actions {
      display: flex;
      gap: 1rem;
      align-items: center;
    }
    .btn-primary {
      font-family: var(--font-display);
      font-weight: 700;
      font-size: 0.85rem;
      background: var(--verde);
      color: var(--negro);
      padding: 1rem 2rem;
      border-radius: 100px;
      text-decoration: none;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 40px rgba(188,255,71,0.35);
    }
    .btn-ghost {
      font-size: 0.8rem;
      color: var(--blanco);
      text-decoration: none;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      opacity: 0.6;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      transition: opacity 0.2s;
    }
    .btn-ghost:hover { opacity: 1; }
    .btn-ghost svg { transition: transform 0.2s; }
    .btn-ghost:hover svg { transform: translateX(4px); }

    .hero-scroll {
      position: absolute;
      right: 4rem;
      bottom: 5rem;
      writing-mode: vertical-rl;
      font-size: 0.65rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      opacity: 0.35;
      z-index: 1;
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    .hero-scroll::after {
      content: '';
      width: 1px; height: 60px;
      background: var(--blanco);
      opacity: 0.3;
      animation: scrollLine 2s ease-in-out infinite;
    }
    @keyframes scrollLine {
      0%, 100% { transform: scaleY(1); opacity: 0.3; }
      50% { transform: scaleY(0.5); opacity: 0.1; }
    }

    /* TICKER */
    .ticker {
      border-top: 1px solid var(--gris2);
      border-bottom: 1px solid var(--gris2);
      padding: 1rem 0;
      overflow: hidden;
      white-space: nowrap;
    }
    .ticker-inner {
      display: inline-flex;
      gap: 0;
      animation: ticker 25s linear infinite;
    }
    .ticker-item {
      display: inline-flex;
      align-items: center;
      gap: 1.5rem;
      padding: 0 2rem;
      font-family: var(--font-display);
      font-weight: 700;
      font-size: 0.8rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      opacity: 0.4;
    }
    .ticker-dot {
      width: 5px; height: 5px;
      border-radius: 50%;
      background: var(--verde);
      opacity: 1;
      flex-shrink: 0;
    }
    @keyframes ticker {
      from { transform: translateX(0); }
      to { transform: translateX(-50%); }
    }

    /* SERVICIOS */
    .section { padding: 8rem 4rem; }
    .section-label {
      font-size: 0.65rem;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      color: var(--verde);
      margin-bottom: 3rem;
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    .section-label::before {
      content: '';
      width: 24px; height: 1px;
      background: var(--verde);
    }
    .section-title {
      font-family: var(--font-display);
      font-weight: 800;
      font-size: clamp(2.5rem, 5vw, 4.5rem);
      line-height: 1;
      letter-spacing: -0.03em;
      margin-bottom: 5rem;
      max-width: 700px;
    }

    .services-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1px;
      border: 1px solid var(--gris2);
    }
    .service-card {
      padding: 3rem 2.5rem;
      border-right: 1px solid var(--gris2);
      position: relative;
      overflow: hidden;
      transition: background 0.3s;
    }
    .service-card:last-child { border-right: none; }
    .service-card::before {
      content: '';
      position: absolute;
      bottom: 0; left: 0; right: 0;
      height: 3px;
      background: var(--verde);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform 0.4s ease;
    }
    .service-card:hover::before { transform: scaleX(1); }
    .service-card:hover { background: var(--gris); }

    .service-num {
      font-family: var(--font-display);
      font-size: 0.65rem;
      letter-spacing: 0.15em;
      color: var(--verde);
      margin-bottom: 2rem;
      opacity: 0.8;
    }
    .service-icon {
      font-size: 2.5rem;
      margin-bottom: 1.5rem;
      display: block;
    }
    .service-name {
      font-family: var(--font-display);
      font-weight: 700;
      font-size: 1.35rem;
      margin-bottom: 1rem;
      letter-spacing: -0.02em;
    }
    .service-desc {
      font-size: 0.9rem;
      line-height: 1.65;
      opacity: 0.55;
    }
    .service-tags {
      margin-top: 2rem;
      display: flex;
      flex-wrap: wrap;
      gap: 0.4rem;
    }
    .tag {
      font-size: 0.65rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      border: 1px solid var(--gris2);
      padding: 0.3rem 0.7rem;
      border-radius: 100px;
      opacity: 0.5;
      transition: border-color 0.2s, opacity 0.2s;
    }
    .service-card:hover .tag {
      border-color: var(--verde);
      opacity: 0.8;
    }

    /* MANIFIESTO */
    .manifesto {
      padding: 8rem 4rem;
      background: var(--verde);
      color: var(--negro);
      position: relative;
      overflow: hidden;
    }
    .manifesto-bg-text {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-family: var(--font-display);
      font-weight: 800;
      font-size: 25vw;
      color: rgba(0,0,0,0.06);
      white-space: nowrap;
      pointer-events: none;
      user-select: none;
    }
    .manifesto-content {
      position: relative;
      z-index: 1;
      max-width: 1000px;
    }
    .manifesto-label {
      font-size: 0.65rem;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      margin-bottom: 2rem;
      opacity: 0.6;
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }
    .manifesto-label::before {
      content: '';
      width: 24px; height: 1px;
      background: var(--negro);
    }
    .manifesto-title {
      font-family: var(--font-display);
      font-weight: 800;
      font-size: clamp(2.5rem, 5vw, 5rem);
      line-height: 1.05;
      letter-spacing: -0.03em;
    }
    .manifesto-title em {
      font-style: italic;
      font-weight: 400;
    }

    /* PROCESO */
    .process {
      padding: 8rem 4rem;
    }
    .process-list {
      margin-top: 2rem;
    }
    .process-item {
      display: grid;
      grid-template-columns: 80px 1fr 1fr;
      gap: 2rem;
      align-items: center;
      padding: 2.5rem 0;
      border-bottom: 1px solid var(--gris2);
      transition: all 0.3s;
    }
    .process-item:hover { padding-left: 1rem; }
    .process-item:hover .process-num { color: var(--verde); }
    .process-num {
      font-family: var(--font-display);
      font-weight: 800;
      font-size: 3rem;
      color: var(--gris2);
      letter-spacing: -0.05em;
      transition: color 0.3s;
    }
    .process-name {
      font-family: var(--font-display);
      font-weight: 700;
      font-size: 1.5rem;
      letter-spacing: -0.02em;
    }
    .process-desc {
      font-size: 0.9rem;
      line-height: 1.6;
      opacity: 0.5;
    }

    /* COMUNIDAD */
    .community {
      padding: 8rem 4rem;
      background: var(--gris);
      position: relative;
      overflow: hidden;
    }
    .community-inner {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 6rem;
      align-items: center;
    }
    .community-stats {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1px;
      background: var(--gris2);
    }
    .stat {
      background: var(--gris);
      padding: 2.5rem;
    }
    .stat-num {
      font-family: var(--font-display);
      font-weight: 800;
      font-size: 3.5rem;
      letter-spacing: -0.04em;
      color: var(--verde);
      line-height: 1;
      margin-bottom: 0.5rem;
    }
    .stat-label {
      font-size: 0.8rem;
      opacity: 0.5;
      line-height: 1.4;
    }

    /* CTA FINAL */
    .cta-section {
      padding: 10rem 4rem;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    .cta-section::before {
      content: '';
      position: absolute;
      top: 50%; left: 50%;
      transform: translate(-50%, -50%);
      width: 600px; height: 600px;
      background: radial-gradient(circle, rgba(188,255,71,0.08) 0%, transparent 70%);
      pointer-events: none;
    }
    .cta-title {
      font-family: var(--font-display);
      font-weight: 800;
      font-size: clamp(3rem, 7vw, 7rem);
      line-height: 0.95;
      letter-spacing: -0.04em;
      margin-bottom: 2.5rem;
      position: relative;
      z-index: 1;
    }
    .cta-title span { color: var(--verde); }
    .cta-subtitle {
      font-size: 1.1rem;
      opacity: 0.5;
      max-width: 450px;
      margin: 0 auto 3rem;
      line-height: 1.6;
      position: relative;
      z-index: 1;
    }
    .cta-email {
      font-family: var(--font-display);
      font-weight: 700;
      font-size: 1rem;
      color: var(--negro);
      background: var(--verde);
      padding: 1.1rem 2.5rem;
      border-radius: 100px;
      text-decoration: none;
      display: inline-block;
      transition: transform 0.2s, box-shadow 0.2s;
      position: relative;
      z-index: 1;
      letter-spacing: 0.03em;
    }
    .cta-email:hover {
      transform: translateY(-3px);
      box-shadow: 0 16px 60px rgba(188,255,71,0.3);
    }

    /* FOOTER */
    footer {
      border-top: 1px solid var(--gris2);
      padding: 3rem 4rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .footer-logo {
      font-family: var(--font-display);
      font-weight: 800;
      font-size: 1rem;
      color: var(--blanco);
      text-decoration: none;
    }
    .footer-logo span { color: var(--verde); }
    .footer-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }
    .footer-links a {
      color: var(--blanco);
      text-decoration: none;
      font-size: 0.75rem;
      opacity: 0.4;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      transition: opacity 0.2s;
    }
    .footer-links a:hover { opacity: 1; }
    .footer-copy {
      font-size: 0.7rem;
      opacity: 0.25;
      letter-spacing: 0.05em;
    }

    /* ANIMACIONES DE ENTRADA */
    .reveal {
      opacity: 0;
      transform: translateY(30px);
      transition: opacity 0.7s ease, transform 0.7s ease;
    }
    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }
    .reveal-delay-1 { transition-delay: 0.1s; }
    .reveal-delay-2 { transition-delay: 0.2s; }
    .reveal-delay-3 { transition-delay: 0.3s; }

    /* RESPONSIVE */
    @media (max-width: 900px) {
      nav { padding: 1.5rem 2rem; }
      nav ul { display: none; }
      .hero { padding: 0 2rem 4rem; }
      .hero-title { font-size: clamp(3rem, 12vw, 5rem); }
      .hero-bottom { flex-direction: column; align-items: flex-start; gap: 2rem; }
      .section { padding: 5rem 2rem; }
      .services-grid { grid-template-columns: 1fr; }
      .service-card { border-right: none; border-bottom: 1px solid var(--gris2); }
      .manifesto { padding: 5rem 2rem; }
      .process { padding: 5rem 2rem; }
      .process-item { grid-template-columns: 50px 1fr; }
      .process-desc { display: none; }
      .community { padding: 5rem 2rem; }
      .community-inner { grid-template-columns: 1fr; gap: 3rem; }
      .cta-section { padding: 6rem 2rem; }
      footer { padding: 2rem; flex-direction: column; gap: 1.5rem; text-align: center; }
      .hero-scroll { display: none; }
    }
  </style>
</head>
<body>

  <!-- CURSOR -->
  <div class="cursor" id="cursor"></div>
  <div class="cursor-follower" id="cursorFollower"></div>

  <!-- NAV -->
  <nav>
    <a href="/" class="logo">insecto<span>.</span>digital</a>
    <ul>
      <li><a href="#servicios">Servicios</a></li>
      <li><a href="#proceso">Proceso</a></li>
      <li><a href="#comunidad">Comunidad</a></li>
      <li><a href="#contacto" class="nav-cta">Hablemos</a></li>
    </ul>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-bg">
      <div class="blob blob-1"></div>
      <div class="blob blob-2"></div>
      <div class="blob blob-3"></div>
      <div class="noise-overlay"></div>
    </div>

    <div class="hero-label">Agencia Creativa Digital — Santiago, Chile</div>

    <h1 class="hero-title">
      Creamos<br>
      <span class="line-stroke">mundos</span><br>
      <span class="line-accent">digitales</span>
    </h1>

    <div class="hero-bottom">
      <p class="hero-desc">
        Somos una agencia que vive dentro de la comunidad digital. Diseñamos, construimos y conectamos marcas con las personas que importan.
      </p>
      <div class="hero-actions">
        <a href="#servicios" class="btn-primary">Ver servicios</a>
        <a href="#comunidad" class="btn-ghost">
          Nuestra comunidad
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
            <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>
    </div>

    <div class="hero-scroll">Scroll</div>
  </section>

  <!-- TICKER -->
  <div class="ticker">
    <div class="ticker-inner" id="ticker">
      <span class="ticker-item"><span class="ticker-dot"></span>Diseño Web</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Branding</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Marketing Digital</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Comunidad</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Redes Sociales</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Formación Digital</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Estrategia Creativa</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Identidad Visual</span>
      <!-- Duplicado para loop infinito -->
      <span class="ticker-item"><span class="ticker-dot"></span>Diseño Web</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Branding</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Marketing Digital</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Comunidad</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Redes Sociales</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Formación Digital</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Estrategia Creativa</span>
      <span class="ticker-item"><span class="ticker-dot"></span>Identidad Visual</span>
    </div>
  </div>

  <!-- SERVICIOS -->
  <section class="section" id="servicios">
    <div class="section-label reveal">Lo que hacemos</div>
    <h2 class="section-title reveal reveal-delay-1">
      Soluciones que mueven el mundo digital
    </h2>

    <div class="services-grid">
      <div class="service-card reveal reveal-delay-1">
        <div class="service-num">01</div>
        <span class="service-icon">◈</span>
        <div class="service-name">Diseño Web & Branding</div>
        <p class="service-desc">
          Creamos identidades visuales y sitios web que no se olvidan. Desde la estrategia de marca hasta cada pixel del producto final.
        </p>
        <div class="service-tags">
          <span class="tag">UI/UX</span>
          <span class="tag">Identidad</span>
          <span class="tag">Logotipo</span>
          <span class="tag">Web Design</span>
        </div>
      </div>

      <div class="service-card reveal reveal-delay-2">
        <div class="service-num">02</div>
        <span class="service-icon">◎</span>
        <div class="service-name">Marketing & Redes Sociales</div>
        <p class="service-desc">
          Estrategias de contenido y campañas que conectan genuinamente con audiencias reales. Sin ruido, solo impacto.
        </p>
        <div class="service-tags">
          <span class="tag">Contenido</span>
          <span class="tag">Campañas</span>
          <span class="tag">Social Media</span>
          <span class="tag">SEO</span>
        </div>
      </div>

      <div class="service-card reveal reveal-delay-3">
        <div class="service-num">03</div>
        <span class="service-icon">⬡</span>
        <div class="service-name">Comunidad & Formación</div>
        <p class="service-desc">
          Construimos espacios donde las personas aprenden, crecen y colaboran. La comunidad digital como motor de transformación.
        </p>
        <div class="service-tags">
          <span class="tag">Talleres</span>
          <span class="tag">Mentorías</span>
          <span class="tag">Comunidad</span>
          <span class="tag">Formación</span>
        </div>
      </div>
    </div>
  </section>

  <!-- MANIFIESTO -->
  <div class="manifesto">
    <div class="manifesto-bg-text">INSECTO</div>
    <div class="manifesto-content">
      <div class="manifesto-label">Nuestro manifiesto</div>
      <h2 class="manifesto-title">
        El mundo digital no espera.<br>
        <em>Nosotros tampoco.</em><br>
        Creamos, conectamos,<br>transformamos.
      </h2>
    </div>
  </div>

  <!-- PROCESO -->
  <section class="process section" id="proceso">
    <div class="section-label reveal">Cómo trabajamos</div>
    <h2 class="section-title reveal reveal-delay-1">Un proceso tan vivo como tu marca</h2>

    <div class="process-list">
      <div class="process-item reveal">
        <div class="process-num">01</div>
        <div class="process-name">Escuchar & Descubrir</div>
        <div class="process-desc">Entendemos tu negocio, tu audiencia y el espacio que quieres ocupar en el mundo digital.</div>
      </div>
      <div class="process-item reveal">
        <div class="process-num">02</div>
        <div class="process-name">Estrategia & Concepto</div>
        <div class="process-desc">Definimos la dirección creativa y la estrategia que va a guiar cada decisión de diseño y comunicación.</div>
      </div>
      <div class="process-item reveal">
        <div class="process-num">03</div>
        <div class="process-name">Diseñar & Construir</div>
        <div class="process-desc">Damos vida al concepto con diseño de alto nivel y desarrollo robusto. Cada detalle importa.</div>
      </div>
      <div class="process-item reveal">
        <div class="process-num">04</div>
        <div class="process-name">Lanzar & Crecer</div>
        <div class="process-desc">Salimos al mundo, medimos, aprendemos y evolucionamos. El trabajo no termina en el lanzamiento.</div>
      </div>
    </div>
  </section>

  <!-- COMUNIDAD -->
  <section class="community" id="comunidad">
    <div class="community-inner">
      <div>
        <div class="section-label reveal">Comunidad primero</div>
        <h2 class="section-title reveal reveal-delay-1" style="margin-bottom: 1.5rem;">
          Somos parte del ecosistema que construimos
        </h2>
        <p class="reveal reveal-delay-2" style="opacity: 0.55; line-height: 1.7; font-size: 1rem; max-width: 400px;">
          No somos una agencia externa que llega con soluciones enlatadas. Vivimos dentro de la comunidad digital, entendemos sus dinámicas y construimos desde adentro.
        </p>
        <a href="#contacto" class="btn-primary reveal reveal-delay-3" style="display: inline-block; margin-top: 2.5rem;">Únete a la comunidad</a>
      </div>

      <div class="community-stats reveal">
        <div class="stat">
          <div class="stat-num">+50</div>
          <div class="stat-label">Proyectos<br>completados</div>
        </div>
        <div class="stat">
          <div class="stat-num">3×</div>
          <div class="stat-label">Crecimiento<br>promedio en redes</div>
        </div>
        <div class="stat">
          <div class="stat-num">100%</div>
          <div class="stat-label">Proyectos<br>entregados a tiempo</div>
        </div>
        <div class="stat">
          <div class="stat-num">∞</div>
          <div class="stat-label">Ganas de seguir<br>construyendo</div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta-section" id="contacto">
    <h2 class="cta-title reveal">
      ¿Listo para<br><span>crear algo</span><br>extraordinario?
    </h2>
    <p class="cta-subtitle reveal reveal-delay-1">
      Cuéntanos tu proyecto. Sin compromisos, sin plantillas. Solo una conversación real.
    </p>
    <a href="mailto:hola@insectodigital.com" class="cta-email reveal reveal-delay-2">
      hola@insectodigital.com
    </a>
  </section>

  <!-- FOOTER -->
  <footer>
    <a href="/" class="footer-logo">insecto<span>.</span>digital</a>
    <ul class="footer-links">
      <li><a href="#servicios">Servicios</a></li>
      <li><a href="#proceso">Proceso</a></li>
      <li><a href="#comunidad">Comunidad</a></li>
      <li><a href="#contacto">Contacto</a></li>
    </ul>
    <span class="footer-copy">© 2025 Insecto Digital</span>
  </footer>

  <script>
    // Cursor personalizado
    const cursor = document.getElementById('cursor');
    const follower = document.getElementById('cursorFollower');
    let mouseX = 0, mouseY = 0;
    let followerX = 0, followerY = 0;

    document.addEventListener('mousemove', (e) => {
      mouseX = e.clientX;
      mouseY = e.clientY;
      cursor.style.left = mouseX + 'px';
      cursor.style.top = mouseY + 'px';
    });

    function animateFollower() {
      followerX += (mouseX - followerX) * 0.12;
      followerY += (mouseY - followerY) * 0.12;
      follower.style.left = followerX + 'px';
      follower.style.top = followerY + 'px';
      requestAnimationFrame(animateFollower);
    }
    animateFollower();

    // Agrandar cursor en hover
    document.querySelectorAll('a, button').forEach(el => {
      el.addEventListener('mouseenter', () => {
        cursor.style.width = '20px';
        cursor.style.height = '20px';
        follower.style.width = '60px';
        follower.style.height = '60px';
      });
      el.addEventListener('mouseleave', () => {
        cursor.style.width = '12px';
        cursor.style.height = '12px';
        follower.style.width = '36px';
        follower.style.height = '36px';
      });
    });

    // Reveal on scroll
    const reveals = document.querySelectorAll('.reveal');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, { threshold: 0.1 });

    reveals.forEach(el => observer.observe(el));

    // Hero reveal inmediato
    document.querySelectorAll('.hero .reveal').forEach(el => {
      setTimeout(() => el.classList.add('visible'), 100);
    });
  </script>

</body>
</html>
